<h1><%= @list.name %></h1>

<%# si mon movie appartient à la liste  %>

<div class="cards py-3">
  <% @list.bookmarks.each do |bookmark| %>
    <h3><%= bookmark.movie.title %></h3>
    <p><%= bookmark.movie.overview %></p>
    <p><%= bookmark.comment %></p>
    <%= link_to "Delete", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Delete ?"} %>
  <% end %>
</div>
<br>

<%= link_to "Back to my lists", lists_path, class: "btn btn-secondary btn-sm" %>
